import datetime
from array import array

print(datetime.date.today())

arr = array('i', [1, 2,
                  3])  # i - means type - small integers (read more in documentation).
# This kind of arrays is faster, good way to optimize code when needed to

